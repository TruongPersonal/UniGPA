import 'package:flutter_test/flutter_test.dart';
import 'package:unigpa/main.dart';

void main() {
  testWidgets('App smoke test', (tester) async {
    // Đây là smoke test cơ bản — sẽ được bổ sung sau
    expect(UniGpaApp, isNotNull);
  });
}
